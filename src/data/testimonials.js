/**
 * Real client feedback only.
 *
 * Paste an Upwork review here as:
 *   { quote: '…', author: 'Client name', role: 'CTO, Company', rating: 5, project: 'Bookora' }
 *
 * While this array is empty the Testimonials section hides itself and the
 * "Upwork track record" panel carries the proof instead — so nothing fake
 * ever renders on the page.
 */
export const testimonials = []
